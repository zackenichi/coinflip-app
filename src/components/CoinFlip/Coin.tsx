import { Grid, Typography } from '@mui/material';
import { FC } from 'react';

const Coin: FC = () => {
  return (
    <Grid container>
      <Grid item xs={12}>
        <Typography>Coin</Typography>
      </Grid>
    </Grid>
  );
};

export default Coin;
